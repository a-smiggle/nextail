import { Button } from '@nextail/core/button';
import type { NextPage } from 'next';
import React from 'react';

const Home: NextPage = () => (
  <div className="flex">
    <Button />
  </div>
);

export default Home;
